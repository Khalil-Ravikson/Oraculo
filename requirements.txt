# requirements.txt — Oráculo UEMA (Sprint 1: langfuse adicionado)
# =================================================================
# REGRA: não altere versões sem aprovação da equipe.

# ── Web Framework ─────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
pydantic-settings>=2.2.1
jinja2>=3.1.3
python-multipart>=0.0.9

# ── Banco de Dados ────────────────────────────────────────────────
redis>=5.0.3
redisvl>=0.17.0
sqlalchemy>=2.0.29
asyncpg>=0.29.0
alembic>=1.13.1

# ── LLM & RAG (LangChain) ─────────────────────────────────────────
google-genai>=0.5.0
langchain-google-genai>=1.0.3
langchain>=0.1.16
langchain-core>=0.1.45
langchain-text-splitters

# ── Tasks ─────────────────────────────────────────────────────────
tenacity>=8.2.3
celery>=5.3.6

# ── Scraping ──────────────────────────────────────────────────────
scrapling>=0.1.1
beautifulsoup4>=4.12.0

# ── Parsers de Documentos ─────────────────────────────────────────
pymupdf
# Mantemos docling (IBM) e marker (ML) para layouts e PDFs complexos
docling
marker-pdf>=0.3.0
llama-parse>=0.4.0
# Usamos extensões específicas do unstructured ao invés de [all-docs] para evitar lentidão
unstructured[docx,pptx]>=0.12.0

# ── Sprint 1: Observabilidade LLM & Métricas ─────────────────────
# Sem Langfuse → tracing desativado silenciosamente (sem erro)
langfuse>=2.0.0
prometheus-client>=0.20.0
scikit-learn>=1.3.0

# ── LangGraph e Persistência (Desativados) ────────────────────────
#langgraph>=0.2.0
#langgraph-checkpoint-redis>=0.4.0