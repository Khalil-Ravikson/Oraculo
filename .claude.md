# Contexto do Projeto Oráculo
Este é um agente de IA corporativo e assistente acadêmico.
Stack principal: Python 3.12.8, FastAPI, LangChain/LlamaIndex, Celery, Alembic e Redis.

# Regras de Desenvolvimento
- **Tipagem:** Use SEMPRE type hints estáticos em todas as funções e métodos.
- **Assincronismo:** Priorize `async/await` para I/O e chamadas de banco.
- **Economia:** Antes de gerar blocos massivos de código, apresente o raciocínio.
- **Aprendizado:** Se eu corrigir um padrão arquitetural ou um erro recorrente seu, atualize este arquivo `.claude.md` automaticamente para incluir a nova regra.
- Vá direto ao ponto, mostre o código sem pedir desculpas.
- **LangGraph — em reavaliação (última atualização 2026-07-31), NÃO aprovado para `main`:** rejeição original (travou em `state`/`builder`) revertida parcialmente após retestes na branch/worktree `langgraph`, principalmente via WhatsApp. Decisão: manter isolado em branch/worktree própria até os pontos "NÃO testado ainda" abaixo serem fechados; só depois disso reabrir a conversa sobre migrar `router/`, `application/runtime/dispatcher.py` ou `agents/sigaa/auth_flow.py`. LangChain (fora do experimento) continua só para embeddings.
  - **Testado e funcionando** (2026-07-31): rotas de fluxo geral (RAG, CRUD, ticket); comando de saída explícito do HITL ("sair"/"cancelar"/"desistir"/"abortar"/"encerrar"/"parar", match exato — ver `_eh_saida()` em `langgraph_experiment/nodes.py`); RBAC portado do fluxo real (`src/agents/tickets/rbac.py::checar_permissao_chamado`) pros nodes de entrada `ticket_ask_tipo`/`crud_ask_campo` (antes disso, o funil LangGraph não checava permissão nenhuma — qualquer role/status conseguia abrir ticket/CRUD); dedup de webhook por `msg_key_id` (`src/application/webhook/webhook_controller.py`) — o Evolution reentrega o mesmo evento com frequência alta (confirmado em produção, praticamente toda mensagem chegava 2x) e sem isso cada duplicata virava uma task Celery independente mexendo no mesmo funil HITL por conta própria.
  - **Bug real encontrado E CORRIGIDO nesta rodada, cuidado ao reabrir**: `_reset_payload_para_rota()` (`dispatcher_langgraph.py`) resetava `ticket_data`/`ticket_error`/`ticket_confirmed` ao iniciar um funil novo, mas não o campo `cancelado` (novo, do comando de saída). Como o checkpoint persiste indefinidamente por `thread_id` (mesmo problema de fundo do item "ATENÇÃO 2" já documentado na docstring do módulo), uma sessão que digitasse "sair" UMA VEZ ficava com `cancelado=True` gravado pra sempre — todo ticket/CRUD seguinte nessa sessão aceitava a 1ª resposta e ia direto pro fim, porque toda edge condicional do funil checa `state.cancelado` antes de qualquer outra regra. Sintoma enganoso: parecia exatamente o bug de concorrência do checkpointer (2º resume "perdendo" o `next`), mas não era — era esse campo vazando entre execuções. **Se esse sintoma reaparecer** (resposta válida do usuário não avança o funil, `state.next` some), checar primeiro se é o mesmo padrão de leak (algum campo novo de estado esquecido no reset) antes de suspeitar do checkpointer de novo.
  - **Teste direcionado 1/2 — múltiplos `interrupt()` no mesmo node (2026-07-31)**: rodado com um node descartável (2 `interrupt()` sequenciais numa única execução, fora dos nodes de produção) contra o checkpointer real (`AsyncRedisSaver`, não `MemorySaver`). Resultado: **não reproduziu o bug catastrófico** dos issues #5074/#133 — o fluxo completou corretamente end-to-end nos 2 resumes, resposta final combinando as duas respostas capturadas na ordem certa. Achado menor: `aget_state().next` reportou `()` (vazio) logo após o 1º resume, mesmo com um 2º interrupt claramente pendente (confirmado pelo 2º resume funcionar) — inconsistência de relatório, não de execução; sem impacto prático porque o design atual (1 interrupt/node) não depende desse cenário mesmo assim. Versões pinadas no momento do teste: `langgraph==1.2.10`, `langgraph-checkpoint-redis==0.5.1`, `langgraph-checkpoint==4.1.1` — provável que o bug catastrófico dos issues já tenha sido corrigido numa versão posterior à que motivou a rejeição original.
  - **Teste direcionado 2/2 — carga concorrente (2026-07-31)**: 5 processos separados (cada um = 1 "worker" com loop persistente próprio, imitando `run_in_worker_loop()`) rodando o funil de ticket completo (5 mensagens/resumes cada) em paralelo, sessões diferentes, mesmo Redis — **zero erros de event loop**, todos completaram normalmente. Teste extra, mais agressivo: 2 chamadas concorrentes respondendo o MESMO interrupt pendente da MESMA sessão, sem nenhum lock — não quebrou (sem exceção, sem `Event loop is closed`), mas causou **last-write-wins silencioso**: as duas respostas receberam a mesma pergunta seguinte como se tivessem sido aceitas, só uma ficou de fato gravada no `ticket_data`, a outra sumiu sem nenhum aviso ao usuário. **Conclusão prática**: o lock por telefone (`lock:msg:{phone}`, TTL 90s, em `process_message_task.py`) não é redundante — é o que evita esse race em produção; não remover/enfraquecer sem substituir por outra forma de serialização por sessão. Com os dois testes direcionados fechados (interrupt duplo + carga concorrente) sem reprodução do bug catastrófico original, o bloqueio técnico que motivou a rejeição do LangGraph parece resolvido no upstream — falta só RBAC testado corretamente na `main` (lacuna preexistente, não é sobre o LangGraph) antes de reconsiderar a conversa de promover pra `main`.
- **LangGraph + Celery — achados técnicos desta branch/worktree `langgraph` (ainda válidos, não descartar):** (1) Recurso async de vida longa (`AsyncRedisSaver`) NUNCA pode atravessar um `asyncio.run()` por task Celery — cada `asyncio.run()` cria um event loop novo, e reusar um client async criado num loop anterior quebra com `Event loop is closed`/`Future attached to a different loop`. Fix: loop persistente por processo worker (criado em `worker_process_init`, `loop.run_until_complete()` em vez de `asyncio.run()`, fechado em `worker_process_shutdown`) — ver `src/infrastructure/celery_app.py::run_in_worker_loop()`. Retestar sob carga concorrente real (múltiplas mensagens simultâneas) antes de considerar resolvido — nota: `CELERY_CONCURRENCY=1` no `.env` faz o container `worker` processar tudo serialmente hoje, então mandar várias mensagens ao mesmo tempo não gera concorrência real nenhuma; precisa de `--concurrency` maior num teste isolado pra estressar isso de verdade. (2) `langgraph-checkpoint-redis` (pacote do checkpointer Redis) tem bugs abertos conhecidos na resumption de múltiplos `interrupt()` pendentes no MESMO node (funciona no 1º resume, quebra no 2º) — [langchain-ai/langgraph#5074](https://github.com/langchain-ai/langgraph/issues/5074), [redis-developer/langgraph-redis#133](https://github.com/redis-developer/langgraph-redis/issues/133). Mitigação atual: 1 `interrupt()` por node (não vários empilhados no mesmo node), ver `langgraph_experiment/nodes.py`. Testar deliberadamente 2+ `interrupt()` no mesmo node antes de confiar que isso não afeta o Oráculo (um node de teste descartável, fora dos nodes de produção, é o jeito de fazer isso sem arriscar o funil real). Versão do pacote pinada em `requirements.txt` por essa área ser comprovadamente instável.
- **`docker-compose.yml`:** `langgraph_experiment/` precisa estar montado como volume (`x-worker-base` e serviço `api`) igual `./src` — sem isso os containers rodam a cópia congelada da imagem e edições locais nunca chegam neles, mesmo com `--force-recreate` (só rebuild de imagem pegaria, o que é fácil de esquecer no meio de um ciclo de teste rápido).
- **`rest_lab/` (branch/worktree `research/rest-mcp-estudos`, a partir de 2026-07-31):** laboratório de estudo de API REST — prova de capacidade técnica, não feature de produto. Roteamento por regex determinístico (`rest_lab/router.py`), não LLM — ver decisão e motivo completo em `notas.md` seção 10.1. **Achado que vale reaproveitar em qualquer decisão futura de tool-calling real**: o Oráculo hoje não tem NENHUM function-calling nativo de LLM funcionando — `src/capabilities/messaging/gmail_tool.py` (`StructuredTool`/`bind_tools` do LangChain) é código morto, sem consumidor; o padrão real usado em produção é sempre saída estruturada forçada (`response_schema` Pydantic via `google.genai`, ver `src/router/llm_fallback.py`). Se algum dia decidir introduzir tool-calling de verdade, o caminho natural é `google.genai` (provider já usado), não LangChain `bind_tools`.
  - **Plugado no WhatsApp real (2026-07-31, mesma sessão)**: todo comando exige prefixo `rest ` (ex: `rest listar usuários`) — evita colidir com pergunta acadêmica real do aluno. `src/application/runtime/dispatcher_langgraph.py::processar()` intercepta ANTES até da checagem de funil HITL pendente, via `rest_lab.router.tentar_rotear()` (devolve `None` se não reconhecer, cai no fluxo normal). Único ponto do núcleo tocado — nenhuma outra lógica de roteamento/RBAC/guardrails alterada. `rest_lab/` precisa do mesmo mount de volume que `langgraph_experiment/` em `docker-compose.yml` (`./rest_lab:/app/rest_lab`, já adicionado em todos os serviços que montam `./src`) e `httpx` foi promovido pra `requirements.txt` (antes só instalado localmente). **Não testado no WhatsApp de verdade ainda** — ambiente de desenvolvimento sem Docker acessível na sessão em que foi implementado; testar depois de rebuild de imagem (dependência nova em `requirements.txt`, `--force-recreate` sozinho não pega).
- **`mcp_lab/` (mesma branch/worktree `research/rest-mcp-estudos`, a partir de 2026-08-01):** laboratório de estudo de MCP (Model Context Protocol) — mesmo espírito do `rest_lab/`, prova de capacidade técnica, não feature de produto. Piloto: servidor MCP hospedado StackExchange da pipeworx.io (`https://gateway.pipeworx.io/stackexchange/mcp`, transporte `streamable-http`), tools consumidas `search_questions`/`get_answers` (das 5 expostas — `stack_get_user`/`list_questions_by_tag`/`stack_tags` não implementadas ainda, mesmo padrão se forem adicionadas). Roteamento por regex determinístico (`mcp_lab/router.py`, prefixo `stack `), não LLM — mesma decisão e mesmo motivo do `rest_lab/` (ver `notas.md` §10.1). Sessão MCP de **vida curta por chamada** (`mcp_lab/clients.py::stackexchange_session()`, abre/inicializa/fecha a cada tool call) — decisão deliberada por menor consumo de RAM e pra evitar reintroduzir a classe de bug de event-loop-persistente-por-worker já sofrida com `AsyncRedisSaver` (ver item LangGraph + Celery acima).
  - **Achado de versão do SDK `mcp` (testado nesta sessão, `mcp==2.0.0`, `2026-08-01`)**: a API pública do client HTTP mudou de nome/assinatura em relação ao que a doc genérica descreve — é `mcp.client.streamable_http.streamable_http_client` (não `streamablehttp_client`) e o context manager devolve só `(read, write)` (2 valores, não 3 como em versões anteriores do SDK). `CallToolResult` usa `is_error` (snake_case), não `isError`. Se atualizar a versão pinada no `requirements.txt`, testar esses três pontos de novo antes de assumir que continuam iguais.
  - **Testado via CLI local contra o servidor real (2026-08-01)**: `python3 -m mcp_lab.run_test` — `stack buscar python asyncio` e `stack respostas <id>` retornaram dados reais da StackOverflow via MCP; `tentar_rotear()` confirmado devolvendo `None` pra mensagem sem prefixo `stack` (não interfere no fluxo normal). Corpo de resposta do StackExchange vem em HTML cru — `mcp_lab/tools.py::_sem_html()` faz strip de tags antes de montar a mensagem do WhatsApp. **Não testado no WhatsApp de verdade ainda** — mesma pendência do `rest_lab/`: precisa rebuild de imagem (dependência nova `mcp` em `requirements.txt`) e `docker compose --profile ... up -d --force-recreate` nos serviços que montam `./mcp_lab:/app/mcp_lab` (mesmo mount adicionado nos mesmos pontos que `./rest_lab`, ver `docker-compose.yml`).
  - **Integração no núcleo**: `src/application/runtime/dispatcher_langgraph.py::processar()`, step irmão do `rest_lab` (mesmo bloco "-1", ver linha ~166), chamando `mcp_lab.router.tentar_rotear()` com `plan_id="mcp_lab"`/`rota="MCP_LAB"`. Prefixos `rest `/`stack ` não colidem, ordem entre os dois steps não importa.
  - **Brave Search + GitHub via gateway pipeworx (2026-08-02) — doc do pipeworx erra os nomes de parâmetro, testado ao vivo pra achar os certos**: `gateway.pipeworx.io/<pack>/mcp` (StackExchange, `brave-search`, `github`) expõe um catálogo GRANDE de tools "de plataforma" (`ask_pipeworx`, `deep_research`, `remember`/`recall`, até `polymarket_*`) junto com as tools específicas do pack — `list_tools()` retorna tudo isso junto, não só as do pack anunciado na doc. Achados por tentativa/erro (SDK `mcp==2.0.0` valida a RESPOSTA contra `output_schema` e estoura `RuntimeError`/`ExceptionGroup` sem expor o payload cru — só dá pra ver acessando `exception.__cause__.instance`, ver sessão de debug se precisar repetir):
    - Autenticação BYO key: **não é** `?_apiKey=` na URL do gateway (como a doc diz) — é o argumento `_apiKey` (camelCase, maiúscula no K) dentro do próprio `call_tool(nome, args)`. `_apikey` (minúsculo) falha silenciosamente como se a chave não tivesse sido passada.
    - `web_search`: parâmetro é `query` (doc do pipeworx dizia `q`). Resposta: `{"query", "altered", "total", "returned", "results": [{"title","url","description","age",...}]}`.
    - `search_repos` (GitHub): parâmetro `query` (bateu com a doc). Resposta: `{"total_count", "incomplete_results", "repos": [{"name","full_name","description","stars","forks","language","url","topics","updated_at","open_issues"}]}` — **chave raiz é `repos`**, não `items`/`repositories`; **`stars`/`url`**, não `stargazers_count`/`html_url`.
    - `get_user` (GitHub): resposta tem `url` (não `html_url`), `bio`/`email`/`twitter` podem vir `null`.
    - `brave_image_search`/`brave_video_search` NÃO existem no proxy do pipeworx (só `web_search`/`news_search`) — busca de imagem implementada como REST direto (`api.search.brave.com/res/v1/images/search`, header `X-Subscription-Token`, mesma `BRAVE_API_KEY`), fora do protocolo MCP. Formato confirmado: `results[0].properties.url` é a URL da imagem de verdade (usável direto em `enviar_midia_url`); `results[0].thumbnail.src` é só a miniatura.
    - Comandos WhatsApp: `stack site <termo>`, `stack imagem <termo>` (manda a imagem como efeito colateral, precisa de `chat_id` — `dispatcher_langgraph.py` passa `user_context.get("chat_id") or session_id`, mesma lição do bug de JID do YouTube), `stack github <termo>`, `stack perfil <usuário>`.
    - **Prefixo `brave` separado de `stack` (2026-08-02)**: usuário achou confuso "stack site"/"stack imagem" (Brave) parecerem comando de StackOverflow — movidos pra prefixo próprio `brave site <termo>`/`brave imagem <termo>`; `stack` ficou só com StackExchange (`buscar`/`respostas`) e GitHub (`github`/`perfil`).
    - **Bug real de regex encontrado E corrigido nesta rodada — não é typo de digitação, é erro conceitual de pluralização**: `imagens?` em regex significa "imagen" + "s" opcional (casa "imagen"/"imagens"), **nunca** "imagem" (com M) — português pluraliza "imagem"→"imagens" trocando M por N antes do S (irregular), diferente do inglês onde só se acrescenta "s". `brave imagem <termo>` (singular, a forma que qualquer usuário real digita) SEMPRE caía no fallback de "não reconheci", por mais óbvio que o comando parecesse certo — só descoberto testando a regex isolada dentro do container (`re.match(r'imagens?', 'imagem...')` → `None`, `re.match(r'imagem', ...)` → match). Fix: `(?:imagem|imagens)` explícito em vez de `imagens?`. **Lição pra qualquer regex nova em português**: nunca assumir que `palavra + "s?"` cobre plural — conferir se a palavra tem pluralização irregular (`-agem`→`-agens`, `-ão`→`-ões`/`-ães`, etc.) antes de usar o atalho `?`.
    - **Testado no WhatsApp de verdade**: `stack buscar`/`stack respostas` (StackExchange, sessão anterior). `brave site`, `brave imagem`, `stack github`, `stack perfil` — testados localmente fora do Docker (script Python direto, `pydantic-settings` instalado no host pra isso) e a regex confirmada dentro do container após o fix; **envio de verdade da imagem via `enviar_midia_url` ainda não confirmado no grupo WhatsApp** (só a busca REST do Brave foi validada ponta a ponta).
  - **Edição de arquivo Python com container já rodando NÃO recarrega sozinha**: o volume mount (`./src:/app/src`, `./mcp_lab`, `./rest_lab`, etc.) deixa o arquivo atualizado no container assim que salvo, mas o processo Celery (`worker`) já em execução carregou os módulos Python uma vez no boot e não recarrega — editar `supervisor.py`/`media_download_service.py`/qualquer coisa importada e testar via WhatsApp sem reiniciar o worker roda o código VELHO em memória, silenciosamente (sem erro nenhum, só comportamento desatualizado — foi exatamente o que aconteceu com a feature de busca de vídeo por termo nesta sessão: regex nova no disco, worker ainda rodando a versão sem ela). Sempre `docker compose restart <serviço>` depois de qualquer edição, antes de testar no WhatsApp de verdade — **atenção**: existem 5 containers de worker, cada um só consumindo sua(s) fila(s) (`worker`: default/admin/notificacoes; `worker_rag`: rag_search; `worker_synthesis`: synthesis; `worker_media`: media; `worker_graph`: graph — ver `arquitetura_oraculo.md` seção 7.2). Reiniciar o container errado (ex.: `worker` quando a task roda em `worker_media`, como `worker_ytb_download`/`worker_insta_download`/`worker_audio_to_text`/`worker_text_to_audio`, todos `queue="media"`) não dá erro nenhum — só continua rodando o código velho silenciosamente na fila que você esqueceu. A lógica de ROTEAMENTO (`router/supervisor.py`, decide qual worker chamar) roda dentro de `process_message_task`, que é `worker` (fila default) — então uma mudança em `supervisor.py` afetando uma task de `worker_media` exige reiniciar OS DOIS containers.
  - **Instalar `mcp` num container já rodando (sem rebuild) exige `-u root` e o venv certo**: o container tem dois Pythons — um "de sistema" (`/usr/local`, com `pip` normal) e o venv da app (`/opt/venv`, sem `pip` embutido, sem permissão de escrita pro usuário não-root). `docker compose exec worker pip install X` instala no Python errado (não quebra na hora, só quando o import falha em produção). Forma que funcionou: `docker compose exec -u root worker pip --python /opt/venv/bin/python3 install X`, depois `docker compose restart worker` (o processo Celery só reimporta módulos no boot). Isso é só atalho de teste rápido — não substitui rebuild de imagem (que já pega o `requirements.txt` certo).
  - **Envio de vídeo/áudio do YouTube pro WhatsApp (2026-08-01)**: decisão explícita de não usar o `yt-dlp-mcp` (pacote Node.js externo, transporte stdio) por restrição de RAM do ambiente de teste — adicionar Node.js + pacotes à imagem foi considerado caro demais só pra esse piloto. Em vez disso, completou-se o caminho que já existia pela metade em produção: `src/router/supervisor.py` já roteava link de YouTube pra `ytb_download` (`worker_media_download.py`), que baixava via `MediaDownloadService` (yt-dlp Python nativo, já em `requirements.txt`) mas só respondia texto com o `file_path` local — nunca mandava o arquivo de verdade. Adicionado: `EvolutionAdapter.enviar_midia_base64()` (mesmo endpoint `sendMedia` de `enviar_midia_url`, mas com o arquivo em base64 no campo `media` em vez de URL — a Evolution API aceita os dois, só não existia esse caminho no código) e `worker_media_download.py::_enviar_para_whatsapp()` (lê o arquivo de `/tmp`, base64, envia, sempre deleta o arquivo local depois — sucesso ou falha). Cap de envio `_MAX_ENVIO_MB = 16` (mais conservador que o cap de download `_MAX_SIZE_MB = 50` do `MediaDownloadService` — WhatsApp/Evolution não é confiável com payload base64 grande). **Fallback pra áudio (mesma sessão, depois do 1º teste real confirmar o cap de 16MB)**: se o vídeo baixado passar de `_MAX_ENVIO_MB`, `worker_media_download.py::_ytb()` apaga o arquivo de vídeo e baixa de novo com `audio_only=True` (mp3, geralmente bem menor) antes de desistir — mensagem final avisa quando foi rebaixado. Efeito colateral bom: a mensagem de "não coube" antes citava um `file_path` que `_enviar_para_whatsapp()` já tinha apagado no `finally` (sempre deleta, sucesso ou falha) — informação enganosa, removida da mensagem. Implementado só pra `ytb_download` (YouTube) — `insta_download` (Instagram) não foi tocado, mas seguiria exatamente o mesmo padrão se decidido depois. **Sem S3/CDN** — decisão explícita desta rodada (custo/complexidade não justificados pra um teste); se o volume/tamanho de mídia crescer, reabrir essa conversa. **Testado com download real fora do Docker (2026-08-01, `yt-dlp==2026.7.4` instalado localmente via pip)**: `svc.download_youtube("ytsearch1:<termo>")` — busca por termo funciona ponta a ponta (baixa o 1º resultado, título/duração/tamanho corretos). **Não testado ainda dentro do container/via WhatsApp de verdade** (send base64 + Evolution API).
  - **Busca por termo, não só URL (mesma sessão)**: `src/router/supervisor.py` ganhou `_RE_YTB_BUSCA` (`buscar (um) vídeo (sobre/de/do/da) <termo>` — verbo explícito, não colide com pergunta real tipo "tem vídeo sobre isso?"), que monta `dag_hint["url"] = f"ytsearch1:{termo}"` — reaproveita o extractor de busca nativo do yt-dlp (`ytsearch1:`), sem API/chave extra, sem servidor MCP externo. `_plano_media` (`planning.py`) não precisou mudar — já lia só a chave `"url"` do dag_hint.
  - **Existe um TERCEIRO caminho de roteamento pra MEDIA_DOWNLOAD, além do Planner (`_plano_media`) — achado real, custou 3 rodadas de debug nesta sessão**: `src/application/runtime/dispatcher.py` tem um bloco "Fast-Path MEDIA_DOWNLOAD" (`if decision.rota == "MEDIA_DOWNLOAD":`, ~linha 304) que despacha a task Celery DIRETO (via `chain(fn.s(event), ...)`), sem passar pelo Planner nem por `_dag_hint_para_rota`. Ele tinha sua PRÓPRIA extração de URL (`re.findall(r'(https?://\S+)', message)`, fallback pra `message` inteira se não achar URL) — corrigido nesta sessão pra também reconhecer `_RE_YTB_BUSCA` e montar `ytsearch1:`. **Lição**: ao mexer em roteamento de mensagem real do WhatsApp (mensagem sem prefixo de comando), sempre checar `dispatcher.py` procurando por lógica "Fast-Path" duplicada antes de assumir que corrigir `router/supervisor.py::_dag_hint_para_rota` é suficiente — mensagem em linguagem natural passa por `router/llm_fallback.py::orchestrate()` (ação `call_media` etc.) dentro de `dispatcher.py`, que tem fast-paths próprios (`fast_greeting`, `fast_media_*`) que podem ignorar o dag_hint calculado por `supervisor.py`.
  - **Bug real encontrado E corrigido nesta rodada** (pré-existente, mas a busca por termo torna muito mais fácil de bater nele sem querer — ex.: buscar "lofi hip hop radio" cai numa live 24/7): `MediaDownloadService._ytb_sync()` checava `duration > _MAX_DURATION_S` **depois** de `extract_info(url, download=True)` já ter baixado o arquivo inteiro — pra uma livestream isso nunca termina. Fix: `match_filter` do próprio yt-dlp (roda depois da extração de metadata, antes do download de verdade começar) rejeitando `is_live`/`was_live` e duração excessiva. yt-dlp não levanta exceção quando `match_filter` rejeita (só loga aviso) — o código checa `os.path.exists(filepath)` pra detectar rejeição e devolver mensagem amigável, em vez de deixar estourar `FileNotFoundError` cru.
- **`docker-compose.yml` — profiles:** todo serviço tem `profiles: ["core"|"monitoring"|"app"|"gateway"]`, mas nada no repo ativa isso por padrão (nem `.env`, nem script). `docker compose up -d` sem `--profile` não sobe nada (erro explícito tipo "no such service"). Rodar sempre com `docker compose --profile core --profile monitoring --profile app --profile gateway up -d ...`, ou setar `COMPOSE_PROFILES=core,monitoring,app,gateway` no `.env` — ainda não decidido qual, ver se o shell do usuário já tem `COMPOSE_PROFILES` exportado (explicaria comandos sem `--profile` funcionando às vezes).

## 3. Repositórios de Benchmarking Homologados

Curadoria validada em 2026-07-19 (varredura GitHub/Perplexity, cruzada com leitura do código-fonte
de cada item antes de homologar — não confiar em README/doc de terceiros sem checar).

### Aprovados (uso pontual e restrito ao escopo indicado — não copiar arquitetura completa)

- **[vvandk/kinit](https://github.com/vvandk/kinit)** — só o desenho de RBAC (menu dinâmico + botão + role-departamento) e o padrão JWT/OAuth2 via `Depends()`. Não usa Celery (usa APScheduler+MongoDB) — ignorar o resto.
- **[deepwiki: lijianqiao/fastapi-admin-v3 RBAC](https://deepwiki.com/lijianqiao/fastapi-admin-v3/3.2-rbac-and-permission-system)** — permission string `módulo:recurso:ação` + dependency factory `check_perm()`, stack mais próxima da nossa que o kinit. Repo pequeno (6★) e fonte é wiki gerada por IA — conferir código real antes de portar.
- **[codingeasypeasy — RBAC dinâmico em FastAPI](https://codingeasypeasy.com/blog/role-based-access-control-rbac-with-dynamic-permissions-in-fastapi-a-comprehensive-guide/)** — permissão condicional via `ast.parse()` (não `eval()`). Só vale a pena se surgir necessidade real de regra por `centro`/`campus`/`setor` além do enum de role atual.
- **[evolution-foundation/evolution-api](https://github.com/evolution-foundation/evolution-api)** — é Node.js/TS, não FastAPI (é o gateway que já rodamos, não um padrão de código a copiar). Usar só a doc de eventos de webhook para checar cobertura em `webhook_controller.py`.
- **[Artigo Jellyfish — LLM-Driven API Orchestration](https://www.jellyfishtechnologies.com/llm-driven-api-orchestration-using-langchain-celery-redis-queue/)** — valida o padrão `FastAPI → Redis Queue → Celery → DB` via `.delay()`. Não adotar o agente LangChain do artigo como decisor de rota.
- **[celery/celery — next-steps.rst](https://github.com/celery/celery/blob/main/docs/getting-started/next-steps.rst)** — doc oficial atual. Ver Canvas (`chain`/`group`/`chord`) como possível simplificação do `dispatcher.dispatch()`.
- **[SteliosGian/fastapi-celery-redis-flower](https://github.com/SteliosGian/fastapi-celery-redis-flower)** — ideia concreta: adicionar Flower ao `docker-compose.yml` para monitorar as 5 filas Celery em tempo real (hoje só Prometheus/Grafana).
- **[Darshangoswami07/agentic-ai-research-platform](https://github.com/Darshangoswami07/agentic-ai-research-platform)** — stack mais próxima da nossa (FastAPI+Celery+Postgres+Redis+RBAC). Repo com 0★, README "enterprise-grade" de projeto pessoal — usar só como inspiração de nomenclatura/estrutura de agentes (Planner/Researcher/Analyst/Critic), nunca como fonte validada. Não portar Qdrant nem LangGraph.
- **[Shy2593666979/AgentChat](https://github.com/Shy2593666979/AgentChat)** — repo com tração real (810★). Útil só como benchmark de nomenclatura de memória em camadas. Usa MySQL/ChromaDB/Vue — nada de infra portável.
- **[note.com — HITL com LangGraph + FastAPI](https://note.com/nzw_/n/n4095231ae48d?hl=en)** — único princípio realmente agnóstico de framework do lote de HITL: separar o nó de aprovação do nó de efeito colateral (idempotência). Conferir se `auth_flow.py` já respeita isso.
- **[LangChain Docs — Multi-agent](https://docs.langchain.com/oss/python/langchain/multi-agent)** — só vocabulário/taxonomia de padrões (router, handoffs, subagents). Nunca adotar o runtime `StateGraph` (ver regra "Sem LangGraph" acima).

### Descartados (não usar — mantido só para não reabrir a discussão)

onelevenvy/flock (reescrito p/ Rust/Tauri, não é mais FastAPI), jerry609/PaperBot (sem parsing de PDF/chunking, é discovery de papers), Abdulbasit110/nextjs-fastapi (sem auth documentada, premissa SPA não bate com hub Jinja2), anarojoecheburua/RAG-with-Langchain-and-FastAPI (demo didático, inferior ao RAG híbrido atual), post LinkedIn Dheerendra Shivhare (sem código), mabbas-23/multi-agent-orchestration-fastapi (2 commits, showcase vazio), PDF arquivado Celery 2.3 (~2011, API obsoleta), prolifics.ai (marketing, sem framework citado), dev.to interrupts/Command e ivrschool/langchain hitl_design_patterns (100% dependentes do runtime LangGraph, descartado — ver regra acima).